<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit User</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-900 text-white min-h-screen flex items-center justify-center p-6">
  <div class="w-full max-w-xl bg-zinc-800 rounded-2xl shadow-xl p-8 transition-all duration-300 hover:shadow-2xl">
    
    <!-- Navigation -->
    <div class="flex justify-end mb-6">
      <a href="/read" class="text-blue-400 hover:text-blue-600 transition duration-200 underline">← Back to Users</a>
    </div>

    <!-- Title -->
    <h1 class="text-4xl font-bold mb-6 text-center tracking-tight">Update User</h1>

    <!-- Image Preview -->
    <div class="flex justify-center mb-6">
      <img src="<%= user.image %>" alt="User Image" class="w-32 h-32 rounded-full border-4 border-zinc-700 shadow-lg object-cover hover:scale-105 transition-transform duration-300">
    </div>

    <!-- Form -->
    <form action="/update/<%= user._id %>" method="post" class="space-y-5">
      <input 
        class="w-full px-5 py-3 bg-zinc-700 border border-zinc-600 rounded-xl focus:outline-none focus:ring-2 focus:ring-pink-400 transition duration-200 placeholder:text-zinc-400"
        type="text" name="name" placeholder="Name" value="<%= user.name %>">

      <input 
        class="w-full px-5 py-3 bg-zinc-700 border border-zinc-600 rounded-xl focus:outline-none focus:ring-2 focus:ring-pink-400 transition duration-200 placeholder:text-zinc-400"
        type="text" name="email" placeholder="Email" value="<%= user.email %>">

      <input 
        class="w-full px-5 py-3 bg-zinc-700 border border-zinc-600 rounded-xl focus:outline-none focus:ring-2 focus:ring-pink-400 transition duration-200 placeholder:text-zinc-400"
        type="text" name="image" placeholder="Image URL" value="<%= user.image %>">

      <input 
        class="w-full py-3 rounded-xl bg-gradient-to-r from-pink-500 to-yellow-400 text-black font-semibold cursor-pointer hover:from-pink-600 hover:to-yellow-500 transition duration-300 shadow-md hover:shadow-lg"
        type="submit" value="Update User">
    </form>

  </div>
</body>
</html>
