<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
     <div class="main w-full min-h-screen bg-zinc-900 p-10 text-white">
            <a class="text-blue-500 inline-block mb-10" href="/">Go Back to Home</a>
           <h1 class="text-3xl tracking-tighter "><%= filename%></h1>
           <p class="text-xl mt-2 font-light "><%= filedata%></p>
     </div>
</body>
</html> -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>View File</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    .fade-in {
      animation: fadeIn 0.5s ease-out both;
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .back-link:hover {
      color: #38bdf8;
      text-decoration: underline;
      transform: translateX(-5px);
    }

    .file-box:hover {
      box-shadow: 0 0 25px rgba(59,130,246,0.15);
      transform: scale(1.005);
    }

    .gradient-header {
      background: linear-gradient(to right, #ffffff, #60a5fa);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }
  </style>
</head>
<body class="bg-gradient-to-br from-zinc-950 to-zinc-900 text-white min-h-screen flex justify-center px-4 py-10 selection:bg-blue-500 selection:text-white">

  <div class="w-full max-w-4xl fade-in">

    <!-- Go Back Button -->
    <a href="/" class="text-blue-400 back-link text-sm mb-8 inline-block font-semibold transition duration-300 ease-in-out">
      ← Go Back to Home
    </a>

    <!-- File Title -->
    <h1 class="text-4xl font-extrabold tracking-tight mb-4 pb-2 gradient-header border-b border-zinc-700">
      <%= filename %>
    </h1>

    <!-- File Data -->
    <div class="file-box transition-all duration-300 bg-zinc-800/50 border border-zinc-700 rounded-xl p-6 shadow-lg text-lg leading-relaxed tracking-wide whitespace-pre-line font-light backdrop-blur-md hover:border-blue-500">
      <%= filedata %>
    </div>

  </div>

</body>
</html>
