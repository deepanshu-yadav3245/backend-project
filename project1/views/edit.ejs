<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
     <div class="main w-full min-h-screen bg-zinc-900 p-10 text-white">
            <a class="text-blue-500 inline-block mb-10" href="/">Go Back to Home</a>
             <form action="/edit" method="post">
                <input class="block w-2/4 rounded-md outline-none text-white px-5 py-2 bg-zinc-800 " type="text" placeholder="previous name" name="previous" value="<%= filename%>">
                <input class="block w-2/4 rounded-md outline-none text-white px-5 py-2 bg-zinc-800 mt-2 " type="text" placeholder="New Name" name="new">
                 <input class="mt-2 bg-pink-600 px-5 py-2 cursor-pointer" type="submit" value="Update Name">
            </form>
     </div>
</body>
</html> -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit Task Name</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    /* Card animation on hover */
    .card-animate {
      transition: transform 0.4s ease, box-shadow 0.4s ease;
    }
    .card-animate:hover {
      transform: translateY(-8px) scale(1.02);
      box-shadow: 0 15px 30px rgba(0, 0, 0, 0.3);
    }

    /* Input focus glow */
    .input-focus:focus {
      box-shadow: 0 0 0 3px rgba(236, 72, 153, 0.4);
      outline: none;
    }

    /* Button hover and scale */
    .btn-animate {
      transition: transform 0.3s ease, background-color 0.3s ease;
    }
    .btn-animate:hover {
      transform: scale(1.05);
      background-color: #be185d;
    }

    /* Slide-in animation */
    .slide-in {
      animation: slideIn 0.6s ease-out both;
    }
    @keyframes slideIn {
      from {
        opacity: 0;
        transform: translateY(40px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>
</head>
<body class="bg-zinc-900 text-white min-h-screen flex items-center justify-center px-4">

  <div class="w-full max-w-xl bg-zinc-800/70 backdrop-blur-lg rounded-xl p-8 shadow-lg card-animate slide-in">

    <!-- Back Button -->
    <a href="/" class="text-blue-400 hover:text-blue-300 mb-6 inline-block transition duration-300">
      ← Go Back to Home
    </a>

    <!-- Title -->
    <h2 class="text-3xl font-bold mb-6 text-white">Edit File Name</h2>

    <!-- Form -->
    <form action="/edit" method="POST" class="space-y-6">

      <!-- Previous Name -->
      <div class="space-y-1">
        <label for="previous" class="block text-sm text-zinc-400">Current Name</label>
        <input
          type="text"
          name="previous"
          id="previous"
          value="<%= filename %>"
          readonly
          class="w-full px-5 py-3 rounded-lg bg-zinc-700 text-white border border-transparent input-focus transition"
        />
      </div>

      <!-- New Name -->
      <div class="space-y-1">
        <label for="new" class="block text-sm text-zinc-400">New Name</label>
        <input
          type="text"
          name="new"
          id="new"
          placeholder="Enter new name"
          required
          class="w-full px-5 py-3 rounded-lg bg-zinc-700 text-white border border-transparent input-focus transition"
        />
      </div>

      <!-- Submit Button -->
      <input
        type="submit"
        value="Update Name"
        class="w-full bg-pink-600 text-white font-semibold py-3 rounded-lg btn-animate cursor-pointer"
      />

    </form>
  </div>

</body>
</html>
